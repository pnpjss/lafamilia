<?php

declare(strict_types=1);

function avatar()
{
    // $image = $_FILES['avatar'];
    // print_r($image);
    // $imageName = $image['name'];
    // $imageTempName = $image['tmp_name'];
    // $imageType = $image['type'];
    // $imageSize = $image['size'];

    // echo $imageName;
    // echo $imageTempName;
    // echo $imageType;
    // echo $imageSize;

    // $imageExt = explode('.', $imageName);
    // $imageActualExt = strtolower(end($imageExt));

    // if ($imageActualExt === 'png' && $imageSize < 1000000) {
    //     $imageNewName = uniqid('', true) . "." . $imageActualExt;
    //     $imageDestination = 'images/' . $imageNewName;
    //     move_uploaded_file($imageTempName, $imageDestination);

    //     $query = "UPDATE users SET avatar = :avatar WHERE username LIKE 'BBB'";
    //     $statement = $pdo->prepare($query);
    //     $statement->bindParam(':avatar', $imageNewName, PDO::PARAM_STR);
    //     $statement->execute();
    // }
}
// avatar();
